# proper terminfo instead of legacy "screen"
set -g default-terminal "tmux-256color"

# truecolor passthrough for kitty
set -as terminal-overrides ",xterm-kitty:RGB"

# pass through escape sequences kitty needs
set -g allow-passthrough on

# reduce esc-key detection delay
set -sg escape-time 10
